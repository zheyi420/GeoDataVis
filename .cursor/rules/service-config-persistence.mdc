---
description: 服务配置本地持久化架构与扩展约束
globs: src/stores/**/*.js, src/views/panels/*.vue, src/App.vue
alwaysApply: false
---

# 服务配置本地持久化

## 存什么

存 Dialog 内**已处理 options**（默认值补全、格式转换后、直接传入 `add*Layer` / `addTerrain` 的纯 JSON），不存原始表单、不存 layerStore（含 `layerInstance`、`providerInstance` 等 Cesium 实例，无法序列化）。

## 数据流

- **服务型 Dialog 确认成功**（WMS/WMTS/3DTiles/Terrain）→ `serviceConfigStore.addServiceConfig(type, layerName, processedOptions, layerId)` → `watch` 自动写入 localStorage
- **App.vue** 在 `isViewerReady` 为 true 且有 `serviceConfigs.length > 0` 时，全屏 Loading 并调用 `restoreAll()`
- **LayerManagerPanel** 在移除、可见性、透明度（3DTiles 除外）、图层顺序、切换地形时，同步调用 `serviceConfigStore` 对应 action（通过 `getConfigIdByLayerId` / `getConfigIdByTerrainId` 反查 configId）

## restoreAll 恢复流程

- **按优先级恢复**：Terrain → WMS/WMTS → 3DTiles，确保依赖顺序正确
- **帧让出**：恢复间插入 `requestAnimationFrame` 避免阻塞主线程
- **3DTiles 加载优化**：恢复时设置 `immediatelyLoadDesiredLevelOfDetail: true` 加速首帧

## restoreAll 恢复约束

- WMS/WMTS：调用 `addXxxLayer` 时传入 `initialState: { visible: config.visible, opacity: config.opacity }`，避免 store 先 `visible: true` 再改 `false` 导致勾选框闪烁
- 3DTiles：传入 `initialState: { visible: config.visible, skipZoom: true }`，不传 opacity；不存储、不恢复透明度，恢复时保持初次加载效果（opacity=1）；`skipZoom: true` 不执行 `zoomToTileset`，避免相机在多个 3DTiles 间反复飞行

## 扩展新 Dialog 类型

**仅限服务型图层**：通过 URL/服务端点加载的图层（WMS、WMTS、Cesium3DTiles、CesiumTerrain）需持久化。在确认成功后调用 `addServiceConfig(type, layerName, processedOptions, layerId)`，`options` 必须为可序列化 JSON（与传给 `add*Layer` 的完全一致）。

**排除文件型图层**：通过「文件」菜单上传的本地文件（GeoJSON、KML 等）**不**需要持久化。刷新后数据不可恢复，且大文件会占用 localStorage。此类 Dialog 无需调用 `addServiceConfig`。
